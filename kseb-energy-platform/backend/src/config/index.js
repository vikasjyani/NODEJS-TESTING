// This file can be used to export all configurations
// For example:
// module.exports = {
//   DatabaseConfig: require('./database'),
//   ServerConfig: require('./server'),
// };

// For now, leave it empty or with a placeholder
module.exports = {
    PORT: process.env.PORT || 5000,
    NODE_ENV: process.env.NODE_ENV || 'development',
    // Add other configurations as needed
};
